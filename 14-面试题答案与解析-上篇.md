# 14｜面试题答案与解析 · 上篇（题 1~21：Binder / 架构 / 启动 / Activity 启动 / 生命周期栈）

> 配套题目见 [13-面试题库与自测清单.md](13-面试题库与自测清单.md)。
> 每个答案含【答案要点】（回答主线）与【加分点】（答出来能拉开差距的细节）。
> 所有行号基于本仓库 AOSP 8.1.0。

---

## 主题 A：Binder 与进程模型（题 1~5）

### 题 1｜Binder 相比共享内存、Socket 等 IPC 的优势是什么？为什么性能好？

【答案要点】
1. **性能好**：传统 IPC（如 Socket）一次数据传递要经过"用户态→内核态→内核缓冲→用户态"，数据拷贝 2 次；Binder 借助内核的 mmap 把接收方的一块用户空间与内核缓冲区映射到同一物理页，发送方拷入内核缓冲的数据接收方**直接可见**，只需 **1 次拷贝**。
2. **安全**：Binder 驱动由内核为每次调用带上调用方的 PID/UID，服务端可以据此做权限校验（AMS 里 `Binder.getCallingUid()`）。
3. **面向对象**：跨进程也能像调用本地方法一样（Proxy/Stub），不用自己拼协议。

【加分点】
- 补充：Binder 还支持**双向调用**（同一连接客户端也能收到服务端的回调，如 IApplicationThread）；
- 能说出"1 次拷贝 vs 2 次拷贝"的对比，面试官会认可你理解到位。

### 题 2｜描述一次 Binder 调用的完整链路（以 App 调用 AMS.startActivity 为例）

【答案要点】
1. App 侧 `ActivityManager.getService()` 拿到 `IActivityManager.Stub.Proxy`；
2. 调用 `proxy.startActivity(...)`：把参数写入 Parcel → `Binder.transact()`（挂起等待）；
3. Binder 驱动把数据送入 system_server 的 Binder 线程池（`binder_thread`），
   唤醒 `Stub.onTransact()`：解包 → 根据 code 分发 → 调用真实 `AMS.startActivity()`；
4. 返回结果打包 → 驱动送回 → Proxy 解包 → 调用方拿到返回值。

【加分点】
- 点出"AMS 方法跑在 **Binder 线程**上，不是主线程"，这解释了为什么 AMS 到处加锁；
- 点出 `Parcel` 是跨进程传输的"装箱单"。

### 题 3｜AIDL 编译会生成哪些类？Stub 和 Proxy 各自做什么？

【答案要点】
1. 会生成一个同名 Java 接口（如 `IActivityManager`），内部含：
   - 方法签名的常量（TRANSACTION_xxx code）；
   - **Stub**（抽象类，继承 `Binder`）：服务端用。核心是 `onTransact()`，按 code 解包并分发到对应方法；
   - **Proxy**（实现接口）：客户端用。每个方法 = 打包 + `transact()` + 解包。
2. Stub 还提供 `asInterface()`：同进程直接返回对象，跨进程返回 Proxy。

【加分点】
- 说出 `Stub.asInterface` 内部判断 `descriptor` / `queryLocalInterface` 的逻辑；
- 8.1 里 AMS 直接 `extends IActivityManager.Stub`（AMS 第 454 行），服务端连"包装"都省了。

### 题 4｜oneway 关键字的作用？哪些场景适合？

【答案要点】
- `oneway` 声明的方法是**异步单向调用**：客户端调用后立刻返回，不等待结果；
- 实现上：`transact()` 带 `FLAG_ONEWAY`，数据进入内核后不等应答，服务端在 Binder 线程池里异步执行。

【加分点】
- 场景举例：`IActivityManager` 里的部分通知类调用、`IApplicationThread.scheduleXxx` 这类"下达命令、不需要回执"的调用适合用 oneway；
- 提醒注意：oneway 调用**没有返回值**，也不能通过跨进程方式拿结果。

### 题 5｜Zygote 为什么用 fork 创建进程？它预加载了什么？

【答案要点】
1. `fork()` 复制父进程，配合 **copy-on-write** 内存共享，启动新进程极快；
2. Zygote 启动时预加载：Java 虚拟机、核心类库、主题/资源等常用框架对象；
3. 这样 fork 出的 App 进程**继承**这些预加载内容，省去重复加载，启动快。

【加分点】
- 说出启动链：init → Zygote → fork SystemServer → AMS 需要时再 `Process.start` → Zygote fork App 进程；
- 点出"App 进程的入口是 `ActivityThread.main`"。

---

## 主题 B：总体架构（题 6~9）

### 题 6｜AMS 运行在哪个进程？核心职责有哪些？

【答案要点】
- 运行在 **system_server** 进程（有特权的系统进程）里；
- 职责：四大组件的调度（Activity/Service/广播/Provider）、进程创建与杀死、任务栈管理、
  权限与安全校验、ANR/崩溃处理、内存（oom_adj）管理。

【加分点】
- 补一句：AMS 通过 `SystemServiceManager.startService(AMS.Lifecycle)` 在开机引导阶段创建（`SystemServer.java` 第 521 行）。

### 题 7｜说出 AMS 内部 6 个以上核心类及各自职责。

【答案要点】
| 类 | 职责 |
| --- | --- |
| `ActivityStarter` | 审批 startActivity：解析 Intent、校验、定任务栈方案 |
| `ActivityStackSupervisor` | 统筹所有栈，协调启动/暂停/恢复，realStartActivityLocked |
| `ActivityStack` | 管理任务栈与 Activity 状态机（resumeTopActivityInnerLocked） |
| `ProcessList` | 计算并下发每个进程的 oom_adj |
| `ActiveServices` | 管理 Service 的启动/绑定/销毁 |
| `BroadcastQueue` | 并行/有序广播的排队与投递 |
| `ProviderMap` | 按 authority 索引 ContentProvider |
| `ActivityRecord/ProcessRecord/ServiceRecord/BroadcastRecord` | 各类"档案" |

【加分点】能说出"档案类"（Record）和"执行类"（Stack/Starter/Queue）的分工，说明你理解架构分层。

### 题 8｜App→系统 与 系统→App 两条 Binder 通道分别是什么？

【答案要点】
- App→系统：**`IActivityManager`**（App 持有 AMS 的 Proxy），用于发起请求（startActivity/startService/registerReceiver...）；
- 系统→App：**`IApplicationThread`**（AMS 持有 App 的 ApplicationThread 引用），用于下达命令（scheduleLaunchActivity/schedulePauseActivity...）。

【加分点】
- 点出"App 进程通过 `attachApplication` 把自己的 ApplicationThread 上报给 AMS"，建立第二条通道；
- 8.1 里 `ApplicationThread` 是 `ActivityThread` 的内部类（第 690 行），实现 `IApplicationThread.Stub`。

### 题 9｜为什么 AMS 里到处都是 synchronized？锁的对象是谁？

【答案要点】
- AMS 的方法大多运行在 **Binder 线程池**的多个线程上，同时有大量并发请求；
- `mProcessNames`、`mLruProcesses`、栈结构等共享数据结构需要互斥保护；
- 锁对象通常是 **AMS 自身**（`synchronized (ActivityManagerService.this)`）或某个内部结构。

【加分点】
- 补充"主线程（system_server 主线程）+ Binder 线程都可能操作同一份数据，所以必须加锁"；
- 能说出锁粒度大导致某些操作耗时，这也是 10+ 拆分服务的原因之一。

---

## 主题 C：AMS 启动（题 10~12）

### 题 10｜系统开机启动流程：Zygote→SystemServer→AMS 是怎么一步步走的？

【答案要点】
1. 内核启动 init 进程；
2. init 启动 **Zygote**（预加载虚拟机/类库）；
3. Zygote fork 出 **SystemServer**；
4. SystemServer.main() → `startBootstrapServices()`：`SystemServiceManager.startService(AMS.Lifecycle)` 创建 AMS；
5. AMS 构造初始化 → `setSystemProcess()` 注册 `"activity"` 服务 → `installSystemProviders()`；
6. `startOtherServices()` 最后调 `AMS.systemReady()` → 启动桌面、发 BOOT_COMPLETED。

【加分点】
- 说出 AMS 是"引导阶段"第一批启动的服务（因为其它服务都要用它）；
- 说出 8.1 已无 `AMS.main()`，入口是 `Lifecycle`（老博客写法是 5/6 时代的）。

### 题 11｜AMS 构造函数做了哪些初始化？

【答案要点】
- 创建 AMS 专属的 Handler/Looper 线程（处理异步消息）；
- 创建 `ActiveServices`、`ActivityStackSupervisor` 等子管家并注入引用；
- 创建 `ProcessList` 并读取 oom_adj 相关系统配置；
- 从 PMS 读取已安装应用信息，初始化 `mProcessNames`、`mLruProcesses`；
- 注册各类系统回调（电源、电池统计、AppOps 等）。

【加分点】点出"构造只是布置办公室，真正营业要等 systemReady"。

### 题 12｜systemReady 做了哪几件大事？为什么 BOOT_COMPLETED 之后 App 才算就绪？

【答案要点】
1. 放行启动过程中"积压"的组件请求（早期广播/服务）；
2. 恢复上次前台应用、启动桌面（Launcher）；
3. 发出 `ACTION_BOOT_COMPLETED` 广播，标记系统就绪（`isSystemReady()` 返回 true）。

【加分点】
- 说明"App 静态注册的 BOOT_COMPLETED 在这之后才被投递"，所以它代表"系统可用"；
- 说明 systemReady 用 `Runnable goingCallback` 回调，让调用方在就绪时继续做事。

---

## 主题 D：Activity 启动全流程（题 13~16）

### 题 13｜简述一次 startActivity 从 App 到 AMS 再到目标 App 的完整流程（冷启动）。

【答案要点】
1. App：`Activity.startActivityForResult` → `Instrumentation.execStartActivity` → `ActivityManager.getService().startActivity`（Binder）；
2. system_server：`AMS.startActivity`（4516）→ `startActivityAsUser`（4525）→ `ActivityStarter.startActivityMayWait`（673）；
3. `ActivityStarter`：解析 Intent（resolveIntent/resolveActivity）→ 权限/用户校验 → 任务栈决策（launchMode）→ 暂停当前前台 Activity；
4. 找进程：`ActivityStackSupervisor.startSpecificActivityLocked`（1560）；
   - 进程不存在 → `AMS.startProcessLocked`（3646）→ `Process.start` → Zygote fork → `ActivityThread.main` → `attachApplication` 报到；
5. `realStartActivityLocked`（1313）→ `app.thread.scheduleLaunchActivity`（1457，Binder 下行）；
6. App：`ActivityThread.handleLaunchActivity`（2833）→ `performLaunchActivity`：反射创建 Activity、attach、onCreate、onStart；`handleResumeActivity`（3608）→ onResume、上屏。

【加分点】把 8 个关键方法名和行号都报出来，基本就是"背得很熟"的水平了。

### 题 14｜热启动相比冷启动，省掉了哪些步骤？

【答案要点】
- 省掉：**进程创建**（Zygote fork、ActivityThread.main、attachApplication 报到）相关步骤；
- 保留：Intent 解析、任务栈决策、暂停前台、realStartActivityLocked → scheduleLaunchActivity。
- 判断方法：logcat 有无 `Start proc ... for activity` 日志。

【加分点】一句话总结：热启动 = 进程已在，直接从"创建 Activity"这步开始。

### 题 15｜startActivityForResult 的结果是怎么送回调用方的？

【答案要点】
1. 启动时调用方把 `resultTo`（自己的 Binder token）和 `requestCode` 交给 AMS；
2. 目标 Activity `setResult` 后 `finish` → `AMS.finishActivity` 找到 resultTo；
3. AMS 把结果（ActivityResult）送回调用的 Activity → 它恢复（onResume）时回调 `onActivityResult`。

【加分点】点出"跨进程回调 = 反向 Binder 调用"，调用方和目标方甚至可能在不同 App。

### 题 16｜为什么说 AMS 是"决策者"、App 是"执行者"？举 3 个证据。

【答案要点】
1. Activity 真实对象由 App 进程的 `ActivityThread` 反射创建，AMS 只发 `scheduleLaunchActivity` 命令；
2. Service/Receiver/Provider 的对象也都创建在 App 进程；
3. AMS 通过 `IApplicationThread` 下发命令，App 收到后在**自己的主线程**执行（Handler 调度）。

【加分点】补充：AMS 管"档案"（ActivityRecord 等），App 管"实例"。

---

## 主题 E：生命周期与任务栈（题 17~21）

### 题 17｜Activity 生命周期由谁驱动？AMS 侧 ActivityState 状态机有哪些状态？

【答案要点】
- 由 AMS 侧 `ActivityStack.resumeTopActivityInnerLocked`（2286）驱动；
- 8.1 的 `ActivityState`（ActivityStack.java 第 218 行）：INITIALIZING、RESUMED、PAUSING、PAUSED、STOPPING、STOPPED、FINISHING、DESTROYING、DESTROYED。

【加分点】能对应说"AMS 状态 → App 回调"（RESUMED→onResume 后、PAUSED→onPause、STOPPED→onStop、DESTROYED→onDestroy）。

### 题 18｜onPause 之后一定会 onStop 吗？onPause 里适合做什么？

【答案要点】
- **不一定**：被半透明 Activity/弹窗遮挡只触发 onPause（仍可见一部分），完全不可见才 onStop；
- onPause 适合：暂停动画、释放独占资源；**不要**做耗时操作（它影响新 Activity 上屏）。

【加分点】onPause 必须快速返回，因为下一个 Activity 要等它完成才上屏（这就是"onPause 里不能开线程等结果"的原因之一）。

### 题 19｜四种 launchMode 的区别？singleTask 启动时会清栈吗？

【答案要点】
| 模式 | 行为 |
| --- | --- |
| standard | 每次新建实例 |
| singleTop | 已在栈顶则不新建，回调 onNewIntent |
| singleTask | 所在 Task 里已有实例则把其上方的 Activity 全部清除，调到栈顶（**会清栈**） |
| singleInstance | 独占一个 Task，全局唯一 |

【加分点】singleTask 的清栈动作在 `ActivityStarter`/栈逻辑里体现；注意 singleTask 跨 App 时会找到对应 taskAffinity 的 Task。

### 题 20｜FLAG_ACTIVITY_NEW_TASK + FLAG_ACTIVITY_CLEAR_TOP 组合的效果？

【答案要点】
- `NEW_TASK`：放到新 Task（或 taskAffinity 匹配的已有 Task）里启动；
- `CLEAR_TOP`：把目标 Activity 之上的全部 Activity 清掉；
- 组合 ≈ **singleTask** 的效果：复用已有实例并清掉上面的。

【加分点】能说明这两个 flag 加不加 `SINGLE_TOP` 时的差异（目标在栈顶时是否重建）。

### 题 21｜旋转屏幕导致 Activity 重建的机制？configChanges 怎么用？

【答案要点】
- 未声明 configChanges 时：系统走 `handleRelaunchActivity` → 旧 Activity `onSaveInstanceState`→onDestroy，新 Activity onCreate 时把保存的 Bundle 还给它（onRestoreInstanceState）；
- 声明 `android:configChanges="orientation|screenSize"` 后：只回调 `onConfigurationChanged`，不重建。

【加分点】说明"决策在系统侧、执行在 App 侧"，以及为何推荐用 ViewModel 存状态而非依赖 onSaveInstanceState 的时序。

---

*下一篇答案见 [15-面试题答案与解析-下篇.md](15-面试题答案与解析-下篇.md)（题 22~48）*
