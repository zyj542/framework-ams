# 11｜调试与实战：把知识变成排查问题的能力

> 一句话总结：**学 AMS 的最高境界，是系统出问题时能自己定位 ——
> 本章教你三板斧：`dumpsys activity`（看状态）、`logcat`（看过程）、`am` 命令（动手操作），
> 再配合断点，就能把前面 10 篇的知识用到真问题上。**

---

## 1. 第一板斧：dumpsys activity（看系统侧的"体检报告"）

所有子命令都基于这个入口，先看总体：

```bash
adb shell dumpsys activity          # 全部信息（很长，通常配合 grep）
```

常用子命令：

| 命令 | 看什么 | 对应文档 |
| --- | --- | --- |
| `dumpsys activity activities` | 当前所有 ActivityRecord / TaskRecord / Stack 的状态 | 04、05 |
| `dumpsys activity processes` | 每个进程的 pid、uid、**oom adj**、procState、跑着哪些组件 | 06 |
| `dumpsys activity services` | 所有 ServiceRecord、绑定关系、是否前台 | 07 |
| `dumpsys activity broadcasts` | 广播队列、历史广播、Registered Receivers | 08 |
| `dumpsys activity providers` | 所有 ContentProviderRecord、谁在用 | 09 |
| `dumpsys activity top` | 当前栈顶 Activity 的详细信息（Intent、taskAffinity 等） | 05 |
| `dumpsys activity oom` | 按 oom_adj 排序的进程列表（排查"谁会被杀"最快） | 06 |

**实测例子（务必亲手敲一遍）：**
```bash
# 看进程的 oom_adj —— 找 Proc # 和 oom adj 两列
adb shell dumpsys activity processes | grep -E "oom adj|Proc #" | head -30

# 看当前任务栈 —— 找 TaskRecord 和 Hist # 0（栈顶）
adb shell dumpsys activity activities | grep -E "TaskRecord|Hist #" | head -30

# 看注册的广播接收者（重点看动态注册的）
adb shell dumpsys activity broadcasts | grep -A5 "Registered Receivers"
```

---

## 2. 第二板斧：logcat（看流程的"行车记录仪"）

### 2.1 常用 Tag（8.1）

| Tag | 内容 |
| --- | --- |
| `ActivityManager` | 进程启动/杀死、Activity 启动、ANR、force-stop 等核心日志 |
| `ActivityStarter` | Activity 启动的详细决策日志（需 debug 开关，eng 版默认开启部分） |
| `ActivityThread` | App 侧执行生命周期回调的日志 |
| `BroadcastQueue` | 广播入队/投递/完成 |
| `WindowManager` | 窗口显示（配合 `Displayed` 日志看首帧） |
| `lowmemorykiller` / `lmkd` | 内存杀进程日志 |

### 2.2 一次冷启动该看到的日志序列（照着对）

```bash
adb logcat -s ActivityManager:I ActivityThread:I
# 预期输出（顺序大致如下）：
# ActivityManager: Start proc 1234:com.example.app/u0a87 for activity ...   ← 进程被拉起（冷启动）
# ActivityManager: START u0 {...} from pid ...                             ← AMS 收到启动请求
# ActivityThread: Calling startActivity() from outside of an Activity      ←（若是外部启动）
# ActivityManager: Displayed com.example.app/.MainActivity: +350ms          ← 首帧完成，耗时 350ms
```

### 2.3 排查经典问题的日志套路

| 问题 | 看什么日志 |
| --- | --- |
| App 启动慢 | `ActivityManager` 里的 `Start proc` 与 `Displayed` 间隔 |
| 进程被杀 | `lowmemorykiller` / `lmkd`（按 adj 杀）或 `ActivityManager`（force-stop/主动杀） |
| Activity 启动失败 | `ActivityStarter`、`ActivityManager` 里 `Unable to start activity ... ActivityNotFoundException` 等 |
| ANR | `ActivityManager` 里 `ANR in ...`，配合 `/data/anr/traces.txt` |
| 广播不触发 | `BroadcastQueue`，看投递到哪个 Receiver、有没有 abort/超时 |

---

## 3. 第三板斧：am 命令（动手"使唤"系统）

```bash
# 启动 Activity（-W 等到首帧完成并打印耗时，-n 指定组件）
adb shell am start -W -n com.android.settings/.Settings

# 强制停止（走 AMS.forceStopPackage，会停掉静态广播/闹钟）
adb shell am force-stop com.example.app

# 发广播（显式：-n 指定组件；-a 指定 action）
adb shell am broadcast -a com.example.TEST -n com.example.app/.MyReceiver

# 杀后台进程（所有 cached 进程）
adb shell am kill-all

# 查看当前 AMS 里的服务列表
adb shell service list | grep -E "activity|window|package"
```

---

## 4. 进阶：给 system_server 打断点

> 仅限 **eng/userdebug 版本**（本仓库构建的是 eng 版）。步骤如下：

1. 手机/模拟器开启 USB 调试并 `adb root`；
2. Android Studio → Run → Attach to Process → 选 **system_server**（pid 通常是 1 号附近的大进程）；
3. 注意：system_server 是系统进程，断点会停住**整个系统**，谨慎使用，且部分代码路径无法命中
   （因为它是预加载进 Zygote 的，需要 `android:debuggable` 或对 `android.os.Process` 的处理）；
4. 更稳妥的替代方案：**在源码里加日志**（`Slog.i(TAG, "...")`）重新编译，比打断点更适合跟流程。

> 经验之谈：学 AMS **先学会加日志看日志**，比折腾断点效率高得多。

---

## 5. 一个完整的实战演练：定位"App 为什么启动很慢"

| 步骤 | 命令/操作 | 目的 |
| --- | --- | --- |
| 1 | `adb logcat -c` 清空日志 | 干净的观测起点 |
| 2 | `adb shell am start -W -n <pkg>/<Activity>` | 拿到 `TotalTime` |
| 3 | 抓 `ActivityManager` 日志 | 区分冷/热启动（有无 `Start proc`） |
| 4 | 若是冷启动，看 `Start proc ... for activity` 出现的时间点 | 进程拉起耗了多少 |
| 5 | `dumpsys activity processes` 看目标进程 oom_adj | 判断是否被降权 |
| 6 | 排查 Application/Activity 的 `onCreate` 里是否有耗时操作 | App 侧原因（主线程） |

把每一步的耗时拆开，就能定位瓶颈在「进程启动」「Application 初始化」还是「首帧渲染」。

---

## 6. 常见面试题自测清单（学完本系列应能答出）

**入门级**
1. AMS 是什么？运行在哪个进程？
2. `ActivityManager.getService()` 返回的是什么？内部做了什么？
3. Binder 调用的基本流程？（Proxy → transact → onTransact → 真实方法）

**进阶级**
4. 一次 startActivity 从 App 到 AMS 到 App 的完整调用链？
5. Activity 生命周期由谁驱动？`onPause` 和 `onStop` 的触发区别？
6. 四种 launchMode 的区别？singleTask 启动时会清栈吗？
7. 进程的 oom_adj 是怎么算的？被前台 App 绑定为什么难被杀？
8. startService 和 bindService 的生命周期差异？`START_STICKY` 的作用？
9. 有序广播如何实现"一个传一个"？如何拦截？
10. 8.0 为什么限制隐式广播的静态注册？

**拔高级**
11. AMS 里为什么到处是锁？`synchronized` 锁的是什么对象？
12. `ApplicationThread` 为什么要把活丢回主线程 Handler？
13. 进程被杀后，Service/Provider 会被自动恢复吗？机制是什么？
14. 如何在 AMS 层新增一个系统服务/自定义组件生命周期？

> 答不出的题目，回到对应文档复习；还不行就在源码里 grep 对应类，亲手追一遍调用链。

---

## 7. 本节小结

```
三板斧：dumpsys activity（状态） + logcat（过程） + am 命令（操作）
进阶  ：断点（eng 版、慎用） / 源码加日志（推荐）
实战  ：先分类问题（启动慢/被杀/不触发） → 再选对应命令/日志 → 拆步骤定位
```

## 8. 动手实验（本周必做）

1. 把第 1 节的 4 条 `dumpsys` 命令全部亲手跑一遍，并对照文档 02~09 解释输出里的关键字段。
2. 用第 2 节的日志序列，完成一次「冷启动观测」，把观察到的 `Start proc` 和 `Displayed` 时间记下来。
3. 用 `am force-stop` 杀掉一个 App，再观察该 App 是否还能收到 `BOOT_COMPLETED` 类广播（体会 force-stop 的语义）。
4. 给 `ActivityManagerService.java` 里 `startActivityAsUser`（第 4525 行）加一行 `Slog.i(TAG_AM, "startActivityAsUser: " + intent);`
   编译并刷入（或至少在源码里推演日志会怎么打），体会"加日志跟流程"的方法。
   不会编译刷机？完整命令在 [16-AOSP环境搭建与编译.md](16-AOSP环境搭建与编译.md) 第 5 节。

---

*上一篇：[10-Binder客户端-ActivityThread与ActivityManager.md](10-Binder客户端-ActivityThread与ActivityManager.md)　|　下一篇：[12-术语表与FAQ.md](12-术语表与FAQ.md)*
