# 17｜Framework 编译命令速查：m / mm / mma / mmm / banchan 一篇讲透

> 一句话总结：**AOSP 的编译命令不是 make 的魔法参数，而是 `build/envsetup.sh` 注入的一堆 shell 函数——
> 选对命令，改一行 AMS 代码的编译时间能从 2 小时缩到 3 分钟。**

> 前置：所有命令都要先 `source build/envsetup.sh && lunch <target>`（见 [16-AOSP环境搭建与编译.md](16-AOSP环境搭建与编译.md)）。
> 每个新终端窗口都要重新 source 一次，否则 `m/mm` 会报 `command not found`。

---

## 1. 一张图看懂命令家族

```
make / m（全量编译，最慢）
 ├── make systemimage   编译某个"目标镜像"
 ├── make services      编译某个"模块"（按模块名）
 │
mm / mmm（只编一个模块，最快）
 ├── mm                 "在哪个目录就编哪个目录的模块"
 ├── mma                mm + 把依赖也一起编（推荐）
 ├── mmm <目录>         指定目录编译
 ├── mmma <目录>        mmm + 依赖（推荐）
 │
banchan <模块名>        只编译某个模块/镜像（soong 新语法，8.1 起）
```

**核心心法：改哪编哪，别全量。** 改 `ActivityManagerService.java` 只需要 `m services` + `m systemimage`,
全量 `make` 是给「首次搭建」和「最终发版」用的。

---

## 2. 命令逐个讲（带适用场景）

### 2.1 `make` —— 全量编译（大炮）

```bash
make -j$(nproc)          # 全量编译所有模块并打包全部镜像（首次/发版用）
make -j16                # 指定 16 个并行任务
make clean               # 清掉所有编译产物（out/ 目录），彻底重来
make clobber             # 比 clean 更狠，连配置缓存一起清
```

| 特点 | 说明 |
| --- | --- |
| 最慢 | 首次 2~4 小时，日常别用 |
| 最全 | 所有镜像 + 所有模块一次搞定 |
| 什么时候用 | ① 首次搭建环境 ② 改动涉及底层（Bionic/ART）③ 要发整包 |

### 2.2 `m` —— make 的快捷方式（推荐日常入口）

`m` 是 envsetup.sh 定义的函数，等价于 `make -j$(nproc)`（自动吃满所有核），
而且**支持带目标**，是日常最常用的：

```bash
m                     # 全量编译（等价 make -j$(nproc)）
m systemimage         # 只重打包 system.img（改 framework/services 后常用）
m bootimage           # 只编 boot.img（内核/ramdisk 相关）
m services            # 只编 services 模块（AMS 所在，改动后必须编它）
m framework           # 只编 framework.jar（framework API 改动）
m sdk                 # 编 SDK（给 App 开发用的 android.jar）
m otapackage          # 打 OTA 升级包
m -j8 services        # 指定并行数 + 目标
```

### 2.3 `mm` / `mma` —— 当前目录模块编译（增量编译主角）

先 `cd` 到模块目录，再执行：

```bash
cd frameworks/base/services          # 先进入模块目录（AMS 在这）
mm                                    # 只编译当前目录的模块
mma                                   # ✅ 推荐：当前模块 + 它依赖的模块一起编
```

| | `mm` | `mma` |
| --- | --- | --- |
| 编译范围 | 仅当前目录模块 | 当前模块 + 所有依赖 |
| 风险 | 依赖没编时产物可能不完整 | 无 |
| 速度 | 最快 | 稍慢一点 |
| 结论 | 不推荐单用 | **日常首选** |

> 加了 `mma` 还是没生效？八成是依赖模块的改动没编进去——`mma` 会重新检查依赖，
> 而 `mm` 不会。这也是「改完代码刷进去没变化」的最常见原因之一。

### 2.4 `mmm` / `mmma` —— 指定目录编译（不用 cd）

不想进目录？直接给路径：

```bash
mmm frameworks/base/services          # 编译 services 目录
mmma frameworks/base/services         # ✅ 推荐：+ 依赖
mmma frameworks/base/core             # 编 framework core（API 改动在这）
```

### 2.5 `banchan` —— 单模块/单镜像编译（soong 新语法）

8.1 引入的新一代编译入口，语义更清晰：

```bash
banchan systemimage                   # 只编译 system.img
banchan framework                     # 只编译 framework 模块
```

### 2.6 附表：`source build/envsetup.sh` 注入的其它实用函数

| 命令 | 作用 | 示例 |
| --- | --- | --- |
| `hmm` | 列出所有可用命令（忘了就敲它） | `hmm` |
| `croot` | cd 回源码根目录 | `croot` |
| `cproj` | cd 回当前项目根目录 | `cproj` |
| `godir <名字>` | 按目录名跳转（模糊匹配） | `godir ActivityManager` |
| `jgrep <正则>` | 在 .java 文件里搜 | `jgrep "class ActivityStarter"` |
| `cgrep` | 在 .c/.cpp/.h 里搜 | `cgrep "binder_ioctl"` |
| `resgrep` | 在资源文件里搜 | `resgrep "app_name"` |
| `mgrep` | 在 Android.mk / bp 里搜 | `mgrep "LOCAL_MODULE"` |
| `gettop` | 打印源码根目录路径 | `gettop` |
| `get_build_var <var>` | 查构建变量 | `get_build_var TARGET_PRODUCT` |
| `tapas <模块>` | 只编译单个 app 模块 | `tapas Settings` 后 `m` |

---

## 3. 「改哪编哪」实战对照表（面试/工作中最高频）

| 你改了什么 | 最快编译姿势 | 产物 |
| --- | --- | --- |
| **AMS / 系统服务**（`ActivityManagerService.java` 等） | `cd frameworks/base/services && mma` → `m systemimage` | system.img |
| framework API/公共类（`frameworks/base/core`） | `mmma frameworks/base/core` → `m systemimage` | framework.jar |
| 单个 App（如 Settings） | `tapas Settings` → `m` | Settings.apk |
| 只改日志、想快速验证 | 改 AMS → `m services` → **`m systemimage-nodeps`**（跳过依赖检查，最快！） | system.img |
| 内核/驱动 | 单独编内核（超出本文范围） | boot.img |

> 🔥 **加日志玩家的秘密武器 `systemimage-nodeps`**：它跳过「检查所有依赖是否最新」的步骤，
> 直接拿现有编译产物重新打包 system.img。改一行日志用它，1~2 分钟出包；
> 但它不会重编你的代码，所以**必须先 `m services` 把改动编进去**，顺序是：
> `m services` → `m systemimage-nodeps`。同理还有 `bootimage-nodeps` / `snod`。

### 完整示例：给 AMS 加一行日志（最快闭环）

```bash
source build/envsetup.sh && lunch aosp_arm64-eng   # 新终端必做
cd frameworks/base/services
vim ../core/java/com/android/server/am/ActivityManagerService.java  # 加 Slog.i(...)
mma                          # ① 编 services 模块（含依赖）
m systemimage-nodeps         # ② 跳过依赖检查直接打包镜像（最快）
emulator -wipe-data          # ③ 跑模拟器（或 fastboot flash system）
adb logcat -s ActivityManager | grep 你的日志
```

---

## 4. 环境变量与加速技巧

```bash
# ccache：编译缓存，二次编译快 5~10 倍（强烈建议）
export USE_CCACHE=1
ccache -M 50G                # 缓存上限 50G（按磁盘调）
source build/envsetup.sh     # 重新加载让缓存生效

# 产物目录换到别的盘（磁盘不够时）
export OUT_DIR=/data/out

# 查看当前构建配置
get_build_var TARGET_PRODUCT
get_build_var TARGET_BUILD_VARIANT
```

---

## 5. 常见困惑速答

| 问题 | 答案 |
| --- | --- |
| `m` 和 `make` 有啥区别？ | `m` 是函数，自动 `-j$(nproc)` 满核并行；`make` 要自己加 `-j` |
| `mm` 编完能直接刷吗？ | 不能——`mm` 只产出模块文件，要刷机还得 `m systemimage` 打包镜像 |
| 为什么 `m services` 后刷入没效果？ | ① 没 `m systemimage` 重新打包 ② 镜像没刷进去 ③ `mma` 依赖没编全 |
| 编完报 `No rule to make target`？ | 目标名写错了：模块用模块名（`services`），镜像用镜像名（`systemimage`） |
| 全量编译太慢怎么办？ | 上 ccache + 日常只用 `mma`/`m systemimage-nodeps` 增量 |
| 忘了命令怎么办？ | `hmm` 一键列出所有命令 |

---

## 6. 本章小结

```
选命令 : 全量=make/m | 单模块=mma/mmma | 打包镜像=m systemimage | 加日志最快=m services + m systemimage-nodeps
选缓存 : ccache（-M 50G）是增量编译的命根子
记口诀 : 改哪编哪、先编模块再打镜像、依赖不全会翻车
```

---

## 7. 动手实验（本周必做）

1. `source build/envsetup.sh` 后敲 `hmm`，把输出的命令和本文第 2.6 节对照一遍。
2. 用第 3 节示例完整跑一遍「加日志 → mma → systemimage-nodeps → logcat 验证」闭环，记录总耗时。
3. 分别用 `mm` 和 `mma` 编一次 `frameworks/base/services`，对比时间，体会依赖检查的代价。

---

*上一篇：[16-AOSP环境搭建与编译.md](16-AOSP环境搭建与编译.md)　|　回到 [README.md](README.md)*
