# 15｜面试题答案与解析 · 下篇（题 22~48：进程 / Service / 广播 / Provider / 客户端 / 实战 / 综合）

> 配套题目见 [13-面试题库与自测清单.md](13-面试题库与自测清单.md)。
> 每个答案含【答案要点】（回答主线）与【加分点】（答出来能拉开差距的细节）。
> 所有行号基于本仓库 AOSP 8.1.0。

---

## 主题 F：进程管理（题 22~27）

### 题 22｜什么是 oom_adj？数值大小代表什么？举出几个典型值。

【答案要点】
- oom_adj = 进程的 OOM 调整值（生存分），**数值越大越先被杀，越小越重要**；
- 典型值（8.1，`ProcessList.java`）：
  - `-900 SYSTEM_ADJ`：system_server，不可杀；
  - `-800 PERSISTENT_PROC_ADJ`：常驻进程；
  - `0 FOREGROUND_APP_ADJ`：前台 App；
  - `100 VISIBLE_APP_ADJ`：可见；
  - `200 PERCEPTIBLE_APP_ADJ`：可感知（后台音乐）；
  - `500 SERVICE_ADJ`：有 Service 的进程；
  - `700 PREVIOUS_APP_ADJ`：上一个用过的 App；
  - `900~906 CACHED_APP_MIN/MAX_ADJ`：缓存进程（最容易被杀）。

【加分点】能背出"0 / 100 / 200 / 500 / 900"这组关键分档，并说出"缓存进程从 906 开始向下杀"。

### 题 23｜oom_adj 是怎么计算出来的？什么叫"adj 传播"？

【答案要点】
- 入口 `AMS.updateOomAdjLocked` → `computeOomAdjLocked`（第 20838 行）；
- 基础分 = 该进程当前"最重要组件"对应的档位（前台 Activity→0、可见→100、前台 Service→0 附近、普通 Service→500...）；
- **adj 传播**：如果进程 A 被进程 B 绑定（Service 绑定、ContentProvider 被用等），
  A 会继承 B 的重要性（clientAdj 传递）——被前台 App 绑定的进程会变得更难被杀。

【加分点】能说出"进程里同时有前台 Activity 和后台 Service 时，取最高的那档"，
以及"算法会遍历 client 链递归打分"（所以 computeOomAdjLocked 里大量找 client 的代码）。

### 题 24｜lmkd 是做什么的？它和 AMS 怎么配合？

【答案要点】
- lmkd（low memory killer daemon）是**内核态守护进程**，监听内存水位；
- AMS 算完每个进程的 oom_adj 后，通过 `ProcessList.setOomAdj`（第 630 行）经 socket 把分数下发给 lmkd；
- 内存紧张时，lmkd **按 oom_adj 从大到小**对进程发 SIGKILL（末位淘汰）。

【加分点】一句话总结分工："AMS 负责打分（绩效考核），lmkd 负责裁员（执行）"；
补充"App 在后台被杀"不是随机事件，是 lmkd 按 AMS 给的分数执行的。

### 题 25｜冷启动和热启动的本质区别是什么？

【答案要点】
- 冷启动：进程不存在 → 需要 Zygote fork 新进程 + ActivityThread.main + attachApplication，再创建 Activity；
- 热启动：进程还活着 → 直接 realStartActivityLocked → scheduleLaunchActivity 创建 Activity；
- 本质区别 = **进程是否需要重建**。

【加分点】用日志区分：有 `Start proc ... for activity` 就是冷启动；也影响启动耗时。

### 题 26｜force-stop 与"后台进程被杀"有什么区别？

【答案要点】
- 后台进程被杀（lmkd）：只是进程没了，下次用到时重新冷启动即可，App 的注册信息（动态/静态广播、闹钟、Job）大多还在；
- `force-stop`（`am force-stop` → `AMS.forceStopPackage`）：AMS 主动终止，并**取消该包的所有待办**（静态广播、闹钟、Job、绑定等），直到用户下次**手动**打开 App 才恢复。

【加分点】"force-stop 后收不到 BOOT_COMPLETED"就是这个语义；这也是很多"杀进程软件"实际只是强制停止的原因。

### 题 27｜为什么播放音乐的后台 App（前台服务）不容易被杀？

【答案要点】
- 播放音乐一般用**前台服务**（`startForeground`），其进程档位接近前台/可感知（adj≈0~200）；
- 8.1 里前台服务进程的 adj 远小于后台 Service（500）和缓存（900+），所以 lmkd 杀人的时候它排在很后面；
- 再加上"前台服务 + 通知"的语义，系统尽量保留以保证用户体验。

【加分点】能同时提到"adj 传播"（被音乐 App 绑定的服务也受益），以及 Android 12+ 对后台启动前台服务的限制（体现版本演进）。

---

## 主题 G：Service（题 28~32）

### 题 28｜startService 的完整流程？

【答案要点】
1. App：`Context.startService` → `AMS.startService`（Binder）；
2. system_server：`ActiveServices.startServiceLocked`（327）→ 查/建 ServiceRecord → `bringUpServiceLocked`（2055）；
3. 目标进程不在 → `AMS.startProcessLocked` 拉进程；
4. `realStartServiceLocked`（2201）→ `app.thread.scheduleCreateService`（2233）→ App 进程 `handleCreateService` → `Service.onCreate()`；
5. 回执后 `scheduleServiceArgs` → 每次 startService 都回调 `onStartCommand`。

【加分点】强调"ServiceRecord 是系统侧档案，真实 Service 对象在 App 进程"；
补充 Service 超时（前台 20s / 后台 200s）→ ANR。

### 题 29｜bindService 的完整流程？onBind 会调用几次？

【答案要点】
1. `ActiveServices.bindServiceLocked`（1228）→ 建 ServiceRecord + ConnectionRecord（记录绑定关系）；
2. 进程不在 → 先拉进程；`scheduleCreateService` + `scheduleBindService`（ActivityThread 第 857 行）；
3. App：`Service.onCreate` → `onBind()` 返回 IBinder → 通过 AMS 回传给调用方 → 回调 `ServiceConnection.onServiceConnected`；
4. **onBind 只在第一次绑定时调用一次**，后续绑定直接复用第一次返回的 Binder；
5. 所有绑定方 `unbind` 后，Service 销毁（onDestroy）。

【加分点】说明 `ServiceRecord.connections`（第 86 行）记录绑定关系；"最后一个连接断开就销毁"是 start/bind 的核心差异。

### 题 30｜startService 和 bindService 的区别？同时用会怎样？

【答案要点】
- startService：与调用方无关，干到被 stopService 才 onDestroy；每次 start 调 onStartCommand；
- bindService：跟随绑定方，所有连接断开就 onDestroy；onBind 返回 Binder；
- **同时用**：Service 生命周期取"最长的那个"——需要所有 startService 被 stop 且所有连接都断开，才会 onDestroy；
  优先"既有 onStartCommand 又绑定了"的混合场景下，只要任何一方还"活着"，Service 就不销毁。

【加分点】能说"混合使用时，stopService 不一定触发 onDestroy，得看有没有连接还绑着"。

### 题 31｜onStartCommand 的四种返回值各代表什么？

【答案要点】
| 返回值 | 含义（进程被杀后） |
| --- | --- |
| `START_STICKY` | 被杀后**会重建**，但重建时 Intent 为 null（适合不需要重启参数的后台任务） |
| `START_NOT_STICKY` | 被杀后**不重建**（适合任务已完成、被杀就算了） |
| `START_REDELIVER_INTENT` | 被杀后**重建并重投递**上一次的 Intent（适合必须完成的任务） |
| `START_STICKY_COMPATIBILITY` | 兼容模式，行为类似 STICKY 但有兼容性限制 |

【加分点】"被杀后是否重启 + 是否带 Intent"两个维度讲清楚，就是满分答案。

### 题 32｜startForeground 为什么要发通知？不发会怎样？

【答案要点】
- 前台服务必须发一个**常驻通知**（`NotificationManager`），让用户"看得见"后台在跑什么；
- 不发通知/通知被移除：Android 会抛 `ForegroundServiceDidNotStartInTimeException` 或触发 `Service.onTimeout` / ANR，系统也会针对"滥用前台服务"做限制（如通知移除后强制停止/降级）。

【加分点】在 8.1 上补充"前台服务使进程 adj 接近前台"，以及后续版本对后台启动前台服务的收紧（体现对版本演进的了解）。

---

## 主题 H：广播（题 33~36）

### 题 33｜动态注册和静态注册的区别？8.0 对隐式广播的限制？

【答案要点】
- 动态：代码 `registerReceiver` → `AMS.registerReceiver`（18712），跟随调用方进程，进程死了就没了；
- 静态：Manifest 声明，由 PMS 扫描后转 AMS 登记，广播来时**先拉进程再投递**；
- 8.0+：**隐式广播（未指定包名/组件）不再支持静态注册**（省电/防骚扰），仅保留白名单例外（BOOT_COMPLETED 等）；显式广播不受限。

【加分点】能列举几个例外广播（BOOT_COMPLETED、LOCKED_BOOT_COMPLETED、MY_PACKAGE_REPLACED...），并解释"拉进程"是静态广播耗电的根源。

### 题 34｜并行广播和有序广播的区别？有序广播怎么实现"一个传一个"？

【答案要点】
- 并行广播（`sendBroadcast`）：进 `mParallelBroadcasts`（97），一次性同时投递给所有接收者，无先后；
- 有序广播（`sendOrderedBroadcast`）：进 `mOrderedBroadcasts`（106），`processNextBroadcast` 一次只投给一个，等它 `finishReceiver` 回执后再传下一个；按优先级排序，接收者可 `abortBroadcast()` 中断、`setResult` 传递数据。

【加分点】说出"有序广播的'一个传一个'靠的是 Binder 回执（finishReceiver）驱动的状态机"。

### 题 35｜广播 ANR 是怎么判定的？前台/后台广播超时分别是多少？

【答案要点】
- 广播投递出去后，如果接收者在规定时间内没有处理完（`onReceive` 未返回/未 finish），AMS 判定 ANR；
- 8.1：前台广播（`FLAG_RECEIVER_FOREGROUND`）约 **10s**，后台广播约 **60s**；
- ANR 后系统会把 `/data/anr/traces.txt` 写入并可能弹 ANR 对话框。

【加分点】说明"超时逻辑在 BroadcastQueue 里用消息（timeout）实现"，并强调 onReceive 里不能做耗时操作。

### 题 36｜什么是 Protected 广播和 Sticky 广播？

【答案要点】
- **Protected 广播**：只有系统能发（系统 Manifest `protected-broadcast` 白名单），防止普通 App 伪造系统事件（如 ACTION_BOOT_COMPLETED）；
- **Sticky 广播**（`sendStickyBroadcast`）：广播内容"粘"在系统里，**之后**注册的接收者立刻能收到最近一次的值（典型：ACTION_BATTERY_CHANGED 电量）。

【加分点】"普通 App 发 BOOT_COMPLETED 会被拒绝"是 Protected 的典型考点。

---

## 主题 I：ContentProvider（题 37~38）

### 题 37｜第一次访问一个 Provider 时系统做了什么？为什么之后访问变快？

【答案要点】
1. `AMS.getContentProviderImpl`（11434）按 authority 查 `mProviderMap`；
2. 没找到 → 建 ContentProviderRecord → 目标进程没启动则拉进程；
3. `scheduleInstallProvider`（ActivityThread 1374）→ `handleInstallProvider`（3055）→ `Provider.onCreate()`；
4. 把真实 `IContentProvider` 的 Binder 回传给调用方；
5. **之后**：调用方缓存了 IContentProvider Binder，直接跨进程调用，不再经过 AMS。

【加分点】"首次访问 = 登记+拉进程+创建；后续 = 直达"，所以多次 query 很快；Provider 的 onCreate 只执行一次。

### 题 38｜Provider 的权限如何控制？宿主进程被杀怎么办？

【答案要点】
- 权限：`android:readPermission` / `writePermission`，在 `getContentProviderImpl` 里做 `checkContentProviderPermission`（11482 附近）校验，没权限抛异常；
- 宿主进程被杀：AMS 收到死亡回调，Provider 记录仍存在；下次有人访问时**重新拉进程 + 重建 Provider（onCreate 再执行一次）**。

【加分点】补充"Provider 的使用关系（ContentProviderConnection）会参与 adj 传播"，频繁被用的 Provider 宿主不容易被杀。

---

## 主题 J：客户端视角（题 39~42）

### 题 39｜ActivityThread 是什么？main() 里做了什么？

【答案要点】
- ActivityThread 是 App 进程**主线程上的总执行者**（不是"Activity 的线程"）；
- main（6459）：创建主线程 Looper（prepareMainLooper）→ new ActivityThread → `thread.attach(false)`（向 AMS 报到）→ `Looper.loop()` 开启主线程消息循环。

【加分点】"主线程"这个概念本身就是"运行着 Looper 的线程"，ActivityThread.main 就是它的入口。

### 题 40｜ApplicationThread 是什么？为什么 scheduleXxx 要丢回主线程 Handler？

【答案要点】
- ApplicationThread（ActivityThread 第 690 行内部类）实现 `IApplicationThread.Stub`，是 App 暴露给 AMS 的反向 Binder 接口；
- 它跑在 **Binder 线程**上，但真正执行组件生命周期的必须是**主线程**（单线程模型，保证 UI 安全）；
- 所以每个 `scheduleXxx` 只做一件事：**把参数封装成消息，发送给主线程 Handler `H`**，然后立刻返回；主线程 `handleMessage` 里再调 `handleLaunchActivity` 等真正的方法。

【加分点】"收口令在 Binder 线程，执行在主线程"这句总结很出彩；能说出 LAUNCH_ACTIVITY / CREATE_SERVICE / RECEIVER 等消息 case 更佳。

### 题 41｜attachApplication 干了什么？什么叫"补做欠的活"？

【答案要点】
- App 进程起来后调 `ActivityManager.getService().attachApplication(mAppThread)`（ActivityThread 6330）→ `AMS.attachApplication`（7215）→ `attachApplicationLocked`（6911）；
- 做三件事：① 记录"该 pid 对应哪个 ApplicationThread"（app.thread=thread）；② 通知创建 Application（scheduleCreateApplication → Application.onCreate）；③ **补做欠的活**：进程启动前 AMS 已经答应要创建 Activity / 启动 Service / 投递广播，现在进程到了，把这些"欠账"补上。

【加分点】"欠的活"= 冷启动场景下，startActivity 请求先到、进程后到，进程报到后 AMS 才执行 realStartActivityLocked。能把 04 的时序图串起来。

### 题 42｜为什么 Activity 生命周期回调都在主线程？

【答案要点】
- AMS 的命令通过 `IApplicationThread.scheduleXxx` 到达 App 进程的 Binder 线程，但被转成消息投到主线程 Handler `H`；
- 主线程 Handler 处理时执行 `handleLaunchActivity/handleResumeActivity` 等，回调才被触发；
- 单线程模型保证 UI 与生命周期操作串行、无并发竞争。

【加分点】能反推："主线程被耗时操作占住 → 消息队列堆积 → 生命周期回调被延迟 → 系统判定 ANR"。

---

## 主题 K：实战排查（题 43~45）

### 题 43｜dumpsys activity 的常用子命令有哪些？各自看什么？

【答案要点】
- `activities`：ActivityRecord/TaskRecord/Stack 状态；
- `processes`：进程 pid、uid、oom_adj、procState、跑着哪些组件；
- `services`：ServiceRecord、绑定关系、是否前台；
- `broadcasts`：广播队列、历史、Registered Receivers；
- `providers`：ContentProviderRecord、谁在用；
- `top`：栈顶 Activity 详情。

【加分点】能记住对应关系（processes→文档06、services→07、broadcasts→08、providers→09），面试时"学以致用"的观感很强。

### 题 44｜如何定位"App 启动慢"的瓶颈？

【答案要点】
1. `am start -W` 拿 TotalTime；
2. logcat 看 `Start proc` 与 `Displayed` 间隔 → 判断冷/热启动；
3. 冷启动再拆：进程拉起耗时 vs Application.onCreate vs Activity.onCreate 耗时（可加 Trace / `debug.hwui` 等）；
4. `dumpsys activity processes` 看目标进程 oom_adj，排查是否被降权。

【加分点】"先区分冷/热，再逐段计时"的方法论比答案本身更值钱。

### 题 45｜如何判断一个 App 是被 lmkd 杀还是被 force-stop 杀？

【答案要点】
- lmkd 杀：logcat tag `lowmemorykiller` / `lmkd` 里有 `Killing 'pkg' ... (adj N)` 日志，且通常是内存紧张时；
- force-stop：`ActivityManager` 日志里 `Force stopping ... package` / `am_force_stop`，且该包后续收不到广播/闹钟；
- 进程死亡回调 `AMS.handleAppDiedLocked` 两侧都会触发，但**恢复策略不同**：lmkd 杀后 Service/Provider 可能自动重建，force-stop 后不会（直到用户手动打开）。

【加分点】能说出"force-stop 的语义是'该包被禁用'，直到手动启动才复活"，这是和 lmkd 杀的最大区别。

---

## 主题 L：综合拔高（题 46~48）

### 题 46｜AMS 与 Android 10+ 的 ATMS 是什么关系？

【答案要点】
- Android 10 起把"Activity 任务栈"相关逻辑从 AMS 拆到 **`ActivityTaskManagerService`（ATMS）**；
- 拆分后：AMS 专注**进程管理**（ProcessRecord、oom_adj、进程生命周期），ATMS 专注**Activity/任务栈**；
- 8.1 里两者合一（都在 AMS 内：ActivityStack/ActivityStarter/ActivityStackSupervisor）。

【加分点】"8.1 是单一大 AMS 的经典形态，学会它再读 10+ 只需平移概念"——体现版本演进意识。

### 题 47｜ANR 有哪几类？分别在什么条件下触发？

【答案要点】
1. **输入事件超时（Input ANR）**：主线程 5 秒内没处理完 Key/Dispatch 输入事件；
2. **广播超时（Broadcast ANR）**：前台广播 10s / 后台广播 60s 内没处理完；
3. **Service 超时**：前台启动 20s / 后台启动 200s 内没完成创建（8.1 逻辑在 `ActiveServices.scheduleServiceTimeoutLocked`，第 1828 行附近）；
4. **Provider 超时**（部分版本）：Provider 首次启动/发布超时。

【加分点】能按"类型 + 超时 + 判定方（AMS 侧定时消息）"讲，配合 `/data/anr/traces.txt` 定位。

### 题 48｜从 startActivity 到 onResume，中间发生了哪些 Binder 调用？（压轴题）

【答案要点】按顺序列出关键 Binder 调用：
1. **App→AMS**：`IActivityManager.startActivity`（发起请求）；
2. **AMS→App（当前前台 Activity）**：`IApplicationThread.schedulePauseActivity`（让位）；
3. （若进程不存在）**AMS→Zygote**：socket 请求 fork（严格说不是 Binder，是 socket，可加分）；
4. **App→AMS**：`IActivityManager.attachApplication`（新进程报到）；
5. **AMS→App（新进程）**：`IApplicationThread.scheduleLaunchActivity`（创建 Activity）；
6. **App→AMS**：`IActivityManager.activityLaunched` / `activityResumed`（回执状态）；
7. **AMS→App**：`IApplicationThread.scheduleResumeActivity`（或由 realStart 内部完成）→ onResume。

【加分点】能画出"两条通道交替使用"的时序（上行请求走 IActivityManager、下行命令走 IApplicationThread），
并指出第 3 步是 socket（Zygote 通道）而非 Binder——这是区分"真懂"和"背模板"的关键细节。

---

*上一篇：[14-面试题答案与解析-上篇.md](14-面试题答案与解析-上篇.md)　|　回到 [13-面试题库与自测清单.md](13-面试题库与自测清单.md)*
